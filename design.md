# A股沪深模拟盘 AI-Agent 设计文档

## 1. 目标

构建一个只操作沪深 A 股股票与沪深交易所 ETF 的模拟盘 AI-Agent。它能从可配置行情数据源获取盘面数据，默认使用 AKShare，保留必盈 API、东方财富等适配器作为可切换来源；随后分析固定股票/ETF 池，按策略执行模拟买卖，收盘后生成中文复盘，并基于历史操作进行受控学习。第一阶段不接真实券商交易，不承诺收益，只做研究、模拟和可审计决策。

## 2. 非目标

第一版不做实盘下单、不接券商账户、不支持港股/美股/期货/期权/可转债、不做高频抢单，也不允许模型绕过风控直接下模拟单。自我学习不等于自动改策略上线；任何策略升级必须经过回放或回测验证，并保留旧版本可回滚。

## 3. 用户体验

系统默认中文化：配置项说明、模拟交易原因、风险拒绝原因、日报、日志摘要、策略学习结论都使用中文输出。代码内部可以使用英文命名，但所有面向用户的界面和报告必须中文。用户主要关心“现在持有什么、为什么买/卖/不动、今天学到了什么”。

## 4. 市场范围

交易宇宙只接受沪深市场代码：上交所 `.SH` 与深交所 `.SZ`。允许 A 股股票与交易所 ETF，例如 `600519.SH`、`000001.SZ`、`510300.SH`、`159915.SZ`。系统拒绝非沪深市场、B 股、港股、美股、期货、期权等标的。买入按 100 股/份整数倍模拟；卖出支持零股一次性清仓规则；股票默认 T+1，ETF 是否 T+0 按品种规则配置。

## 5. 数据源设计

数据层使用适配器模式，不让策略直接调用具体行情服务。适配器输出统一行情结构：代码、名称、交易所、时间戳、最新价、开高低昨收、涨跌幅、成交量、成交额、买卖盘口、数据源、延迟与新鲜度。默认行情源与历史 K 线源为 AKShare；必盈 API 和东方财富公开适配器保留为可选 fallback；后续接入 MySQL、其他行情 API 或授权数据服务时，只需新增 provider 并在配置中切换。

## 6. 数据可靠性

每条行情都要标记来源和获取时间。若实时数据超出新鲜度阈值，AI-Agent 可以继续分析，但必须阻止新模拟订单，并在报告中说明“因行情过期未执行”。如果主数据源失败，系统尝试备用数据源；如果所有来源失败，则当天对应标的只能观望。

## 7. 策略分析

策略引擎读取行情、分钟线、日线、持仓和历史决策，输出标准信号：买入、加仓、减仓、清仓、持有、观望。每个信号必须包含置信度、目标仓位、证据、触发规则、风险提示和策略版本。第一版策略升级为多指标综合策略，而不是单一趋势过滤。

多指标策略分三层：第一层识别市场状态，判断标的处于趋势、震荡、弱势或高波动状态；第二层计算技术指标评分；第三层结合持仓和风控生成目标仓位。技术指标包括 SMA/EMA 均线、MACD、RSI、布林带、ATR、成交量比率和可选 VWAP。单个指标只能加减分，不能独立触发下单。

建仓逻辑：价格位于中期均线上方、短中期均线多头排列、MACD 动能改善、RSI 不处于明显超买区、成交量确认时，才允许产生买入信号。加仓逻辑：已有仓位盈利或趋势延续，价格回踩关键均线但未破坏趋势，综合评分进一步提高时才允许加仓。减仓逻辑：RSI 过热、价格触及布林带上轨且动能走弱、MACD 柱体收缩、放量冲高回落或跌破短期趋势时触发。清仓逻辑：跌破中期趋势、ATR 风险扩大、最大回撤触发或多指标同时转弱。

仓位不再是固定值，而是由评分与波动共同决定：评分越高可用目标仓位越高，ATR 或波动率越高目标仓位越低。策略只输出建议目标仓位，例如 0%、20%、40%、60%；最终是否执行仍由风控层决定。

## 8. 量化策略库

除技术指标综合策略外，系统加入可回测、可解释、可开关的量化策略库。量化策略不直接下单，而是输出标准化信号、目标仓位和证据，由策略聚合器统一评分，再交给风控层审批。第一版优先实现适合固定 ETF 池的策略，避免依赖全市场选股和复杂基本面数据。

第一批量化策略包括：时间序列动量策略，观察标的自身过去 N 日收益，强势延续时提高目标仓位，弱势转负时降仓；均值回归策略，用价格相对均线或布林带 z-score 判断短期过度偏离，在趋势未破坏时低吸或减仓；双 ETF 相对强弱轮动策略，在 `588170.SH` 与 `588200.SH` 之间比较近端收益、波动调整收益和回撤，优先配置更强标的；波动率目标仓位策略，根据 ATR 或历史波动率调节仓位，高波动降仓，低波动允许提高仓位；回撤止损/再入场策略，出现策略级回撤或趋势破位时降低风险，等待重新满足条件后再入场。

策略聚合器采用“投票 + 权重 + 风险惩罚”。例如趋势动量、均值回归和相对强弱可以分别给出 `-2` 到 `+2` 的分数；波动率与回撤模块只负责降权或禁止加仓。多个策略一致时提高置信度；策略冲突时保守处理，优先持有或降低目标仓位。每次决策必须写明哪些量化策略支持、哪些反对，以及最终为何采用某个仓位。

参数不能拍脑袋上线。每个量化策略必须有可回放参数：观察窗口、持有窗口、阈值、止损线、仓位档位和冷却期。学习模块可以提出参数调整，但必须经过历史回放，比较收益、回撤、换手率、胜率、盈亏比和最大连续亏损，并保持“待人工确认”。

## 9. 风控与决策

策略只提出建议，风控层决定是否允许执行。核心限制包括单标的最大仓位、总仓位上限、最小现金比例、单日最大交易次数、最大回撤、冷却期、涨跌停附近限制、数据新鲜度、买入 100 股/份整数倍、股票 T+1 可卖数量。所有拒绝都要记录中文原因。

## 10. 模拟盘

模拟盘维护现金、持仓、可卖数量、成本价、市值、浮动盈亏、已实现盈亏、手续费、滑点和订单状态。订单生命周期包括创建、风控通过、模拟成交、部分成交、拒绝、取消。成交价第一版可使用最新价加滑点，后续可升级为盘口撮合或分钟线回放。

## 11. 收盘复盘

每个沪深交易日收盘后生成中文日报。日报包含：账户总资产、现金、总仓位、每个股票/ETF 的数量、成本、市值、仓位、浮盈浮亏；当天是否建仓、加仓、减仓、清仓；每笔操作的价格、数量、仓位变化和执行逻辑；未执行信号与风控拒绝原因；明日关注点。

## 12. 自我学习

学习模块把“当时为什么这么做”和“后来结果如何”连接起来。它统计策略胜率、盈亏比、最大回撤、信号后 N 日表现、错误类型和错过机会。学习模块可以提出参数调整建议，但必须先在历史回放中优于当前版本，且默认需要用户确认后才能启用。

学习模块还要按指标维度复盘：哪些买入来自趋势确认，哪些来自超跌反弹，哪些减仓来自过热或动能衰减。它应统计每个指标组合的后续收益与回撤，识别“误买、早卖、追高、错过趋势、震荡假突破”等错误类型。策略优化建议只能调整阈值、权重、冷却期和仓位上限，不能绕过模拟盘和风控。

学习模块还应按量化策略归因：分别统计时间序列动量、均值回归、相对强弱轮动、波动率仓位和回撤控制对组合收益和风险的贡献。若某策略在最近样本中贡献为负，系统只能建议降低权重或进入观察状态，不能自动删除或反向使用该策略。

## 13. 推荐架构

第一版使用 Python，适合 A 股数据生态和量化回测。模块划分为 `data`、`storage`、`universe`、`features`、`strategy`、`risk`、`paper_broker`、`journal`、`learning`、`app`。本地存储使用 SQLite 起步，数据访问通过 `MarketDataStore` 接口隔离；后续切换 MySQL、PostgreSQL 或其他数据库时，只需实现同一仓储接口，不改策略、风控和模拟盘。配置使用 JSON/YAML 兼容结构，日报使用 Markdown，方便先跑通闭环。

历史 K 线由配置中的 `history_provider` 接入，当前默认使用 AKShare，必盈 API 或东方财富历史接口可按配置切回。同步流程为：按固定标的池拉取 K 线，解析为统一 `Bar` 模型，以 `(symbol, interval, timestamp)` 作为唯一键写入 SQLite，重复同步时使用 upsert 更新。策略运行时从仓储读取最近一段 K 线作为技术指标和量化策略输入。

## 14. 运行节奏

盘前加载固定股票/ETF 池、资金和策略版本。盘中按配置间隔拉取行情、生成特征、运行策略、执行风控、写入模拟订单。收盘后刷新最终行情、结算持仓、生成日报、执行学习复盘。所有时间统一使用 `Asia/Shanghai`。

## 15. 可验证标准

完成第一版后，应能配置一个或多个沪深 A 股/ETF，拉取至少一个数据源的实时行情，在无真实资金风险下完成买入、加仓、减仓、持有和风控拒绝模拟，并生成全中文日报。测试必须覆盖代码范围过滤、数据过期、策略信号、风控拒绝、模拟成交、收盘报告和学习建议。

## 16. 关键取舍

最小可行版本优先做“稳定模拟闭环”，而不是一次性做复杂策略库。数据源优先支持有授权的正式接口；公开网页行情可以作为可替换适配器，但不能成为唯一可信依赖。AI 模型负责解释、总结和提出学习建议；金额、仓位、成交和风控计算全部由确定性代码完成。

技术指标用于提高决策结构化程度，但不作为收益保证。RSI、MACD、布林带等指标本质上来自价格和成交量的再计算，可能滞后或产生假信号。因此第一版采用多指标确认、风控优先、仓位分层和人工确认策略升级，避免“单指标一票通过”。

量化策略也不作为收益保证。动量、反转、配对、因子和波动率控制都可能随市场环境失效，且公开行情、交易成本、滑点和样本外表现会影响结果。因此第一版只允许在模拟盘中运行，并要求每个策略提供回测指标、风险解释和人工确认。

## 17. 已确认决策

1. 第一版默认数据源切换为 AKShare；必盈 API 和东方财富公开适配器保留为可选 fallback。
2. 初始模拟资金启用 100 万人民币。
3. 第一批固定标的为 `588170.SH` 与 `588200.SH`。
4. 日报保存为本地 Markdown 文件。
5. 策略升级必须由用户人工确认后才能启用。
6. 操作策略采用多指标综合评分：均线/EMA、MACD、RSI、布林带、ATR、成交量比率和可选 VWAP。
7. 量化策略库包含时间序列动量、均值回归、双 ETF 相对强弱轮动、波动率目标仓位和回撤止损/再入场策略。

## 18. 实时分析与 Web 驾驶舱

系统新增本地 Web 驾驶舱，用于展示模拟盘的实时交易、持仓、收益分析、策略回测和盈亏结构。Web 层只读取仓储与分析服务，不直接调用行情接口或修改策略参数。首页通过 `/api/dashboard` 拉取 JSON 数据，包含账户概览、实时持仓、最近成交、当日决策、权益曲线、周期收益率、大盘指数对比、盈亏排行榜、盈亏日历和回测优化记录。

收益分析以收盘资产快照为准，按每日、每周、每月、每年计算收益率。大盘对比指数配置化，默认包含上证指数、深证成指、沪深300、创业板指和科创50。指数历史 K 线使用 AKShare 同步并写入统一 `bars` 表，页面绘制归一化收益折线，用于比较 AI-Agent 与主要大盘指数表现。

盈亏排行榜按证券维度计算证券名称、代码、盈利金额、持仓天数和收益率，页面表头支持排序。盈亏日历按日、月、年聚合资产快照差额，用于定位连续盈利、连续亏损和大额回撤区间。

## 19. 自动回测优化

系统新增自动回测优化能力，第一版采用可解释的参数网格搜索：观察窗口、动量阈值、目标仓位组合会在本地历史 K 线上回放，计算收益率、最大回撤、胜率、盈亏比、换手率和最大连续亏损。优化结果写入 `backtest_runs` 表，并在 Web 页面展示。

自动回测只生成“候选参数”和学习建议，不会自动改配置、不会绕过人工确认，也不会影响当前实时盯盘策略。后续可以在同一接口下扩展更多策略族，例如均值回归参数、相对强弱轮动窗口、波动率目标仓位和回撤止损线。

## 20. 参考来源

- [同花顺 iFinD 数据接口手册](https://quantapi.10jqka.com.cn/gwstatic/static/ds_web/quantapi-web/help-center/manual.html)：支持实时行情 `real_time_quotation` 与日内快照 `snap_shot` 等接口。
- [东方财富 Choice 量化接口](https://quantapi.eastmoney.com/) 与 [Choice 数据服务](https://choice.eastmoney.com/product/datacenter)：提供量化接口与金融数据服务。
- [上海证券交易所交易规则（2026 年修订）](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml)：包含交易时间、回转交易、申报数量等规则。
- [深圳证券交易所交易规则（2026 年修订）发布通知](https://www.szse.cn/lawrules/rule/trade/current/t20260424_620190.html)：规则自 2026-07-06 起施行。
- [Fidelity 技术分析说明](https://www.fidelity.com/learning-center/trading-investing/technical-analysis/what-is-technical-analysis)：说明 RSI、MACD、布林带、成交量、支撑阻力等指标用途。
- [Zerodha Varsity 技术指标说明](https://zerodha.com/varsity/chapter/technical-indicators/)：强调指标有领先/滞后差异，且不应作为单一决策来源。
- [Jegadeesh 与 Titman 动量研究](https://www.researchgate.net/publication/4992307_Returns_to_Buying_Winners_and_Selling_Losers_Implications_for_Stock_Market_Efficiency)：研究买入过去赢家、卖出过去输家的中期动量效应。
- [De Bondt 与 Thaler 过度反应研究](https://onlinelibrary.wiley.com/doi/10.1111/j.1540-6261.1985.tb05004.x)：研究市场过度反应与长期反转现象。
- [Gatev、Goetzmann 与 Rouwenhorst 配对交易研究](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=1095996)：研究相对价值配对交易规则。
- [Moskowitz、Ooi 与 Pedersen 时间序列动量研究](https://colab.ws/articles/10.1016%2Fj.jfineco.2011.11.003)：研究资产自身过去收益对后续仓位方向的时间序列动量。

## 20.1 证券目录与基准数据同步

证券代码搜索使用 SQLite 中的 `instrument_catalog`，而不是在 Web 请求中拉取或在 Agent 内存中长期缓存全市场列表。实时盯盘首次运行时和 Web 服务启动时都会在后台尝试同步；生产运行中，监控器每天仅触发一次目录与指数历史同步。目录同步会用当日成功结果整体替换，避免名称和资产类型长期陈旧。

AKShare 的目录同步支持分资产类型的备用列表源：股票优先东方财富列表、失败时使用腾讯列表；ETF 优先东方财富列表、失败时使用同花顺列表。外部源的 `sh600519`、`sz000001` 代码会规范化为统一的 `.SH`、`.SZ` 格式。

大盘比较只渲染实际同步到 `bars` 的指数 K 线。若某指数尚未同步，接口返回“待同步”状态，页面提示缺失项但不会生成全零的伪曲线。指数历史获取失败时，AKShare 会依次尝试配置的主函数及腾讯等备用函数。

## 20.2 收益分析交互

盈亏分析支持“当年”“当月”和自定义日期区间。每次切换均由服务端重新读取该区间的账户快照和指数 K 线，并把区间起点归一化为 0 收益率。图表下方展示 Agent 对五个大盘指数的最新跑赢、跑输或持平结果：以 Agent 与该指数最新共同日期的收益率差计算，并用以中线为基准的红绿进度条呈现差值方向和幅度。

观察池的删除操作采用 DOM 事件监听，避免动态 HTML 中内联参数转义造成按钮失效。删除默认配置标的会写入 SQLite 排除表；已有实际持仓的标的继续由服务端拒绝删除。

---

## 21. Web 驾驶舱视觉重构（待确认）

### 问题陈述

现有看板偏向红色装饰与独立卡片，实时交易、策略理由和风险状态缺少明确阅读优先级。新版应让 A 股模拟盘用户一次扫视就能判断账户状态、当日操作与相对大盘表现。

### 目标用户与单一任务

面向日常查看模拟盘的 A 股投资者。页面的单一任务是：快速回答“账户现在怎样、Agent 正在做什么、相对市场表现如何，以及下一步是否需要人工确认”。

### 方案比较

**方案 A：蓝色交易工作台（推荐）**

以蓝色作为信息与操作主色，红色代表盈利、绿色代表亏损。采用全宽栅格、浅色数据面板和功能性的决策轨道，阅读路径从账户状态到策略行为再到复盘分析。适合持续盯盘与移动端查看。

**方案 B：深色行情终端**

高密度深色底与荧光行情色，适合专业交易终端。它能容纳大量实时字段，但长时间阅读压力较高，也会削弱日报、回测和人工确认的可读性。

**方案 C：研究报告式页面**

以大面积留白和分析图表为主，适合收盘复盘与策略说明。它不够强调盘中状态和模拟执行，因此不作为主看板方案。

### 推荐方案

采用方案 A。蓝色承担导航、筛选、焦点和数据结构，红绿只用于损益结果，避免让颜色同时承担状态与情绪。页面使用 100% 可用宽度与弹性栅格，不设置强制内容最大宽度。

### 视觉令牌

| 角色 | 名称 | 色值 | 用途 |
| --- | --- | --- | --- |
| 主色 | 交易蓝 | `#1769E0` | 导航、主按钮、选中态、焦点 |
| 强调 | 亮蓝 | `#2E90FA` | 图表主线、实时状态、交互悬浮 |
| 文字 | 深海墨 | `#10233F` | 标题、关键金额、表格正文 |
| 背景 | 雾蓝白 | `#F4F8FD` | 页面底色与分区层次 |
| 分隔 | 冷灰蓝 | `#D9E4F2` | 表格线、控件边界、网格 |
| 盈利 | A 股红 | `#D92D20` | 正收益、盈利金额、加仓结果 |
| 亏损 | A 股绿 | `#039855` | 负收益、亏损金额、减仓风险 |

标题使用 `HarmonyOS Sans SC`、`PingFang SC`、`Microsoft YaHei` 的本机优先栈；金额和比率使用 `DIN Alternate`、`Roboto Mono` 的等宽回退栈。圆角统一为 6px，避免大圆角卡片感。

### 信息架构

桌面端按“状态、执行、归因”排列：

```text
顶栏：StockAI | 交易日状态 | 数据源与刷新时间 | 交易看板 / 回测记录
账户总览带：总资产、当日盈亏、累计收益、仓位、可用现金、风险等级
主区左（8 列）：盈亏分析与大盘基准对比折线图
主区右（4 列）：Agent 决策轨道、下一次检查、人工确认提示
次区左（8 列）：实时持仓与最近模拟成交
次区右（4 列）：盈亏日历
底区：盈亏排行榜
独立 Tab：回测记录、策略参数、状态与批量确认
```

移动端依照同一阅读顺序纵向排列；顶栏保留市场状态和刷新入口，图表横向信息改为可收起图例，持仓表以证券名称和损益为主列，其余字段按需展开。

### 页面与组件规则

1. 顶栏采用白底、细蓝下边线；品牌是首屏的明确标识，导航只保留“交易看板”和“回测记录”。
2. 账户总览是一条连续的指标带，不拆为装饰卡片。盈利红、亏损绿，其他数字使用深海墨。
3. 盈亏分析默认显示 Agent、上证指数、深证成指、沪深 300、创业板指和科创 50；所有曲线按选定起点归一化。图例可点选隐藏，基准线使用不同蓝灰色与线型，Agent 线使用亮蓝色加较高线宽。
4. 决策轨道是本次设计的标志元素：以时间顺序展示“数据刷新、策略信号、风控结论、模拟订单、人工确认”五类事件。它是可操作的状态轨迹，不作装饰动画。
5. 实时持仓按仓位权重排序；名称、代码、仓位、成本、现价、浮盈亏和策略状态在一行完成扫描。移动端固定名称与盈亏两列。
6. 盈亏日历保持年/月切换和收益额/收益率切换。月视图完整填充当月空白格，年视图完整展示 12 个月；正负背景仅做低饱和提示，数值仍以红绿为准。
7. 盈亏排行榜以表格承载排序，不再模拟列表。默认按盈利金额降序，列为证券、代码、盈利金额、持仓天数和收益率。
8. 回测记录独立为 Tab，按策略、参数、核心指标、状态、操作展示；“待人工确认”使用蓝色信息徽标，确认按钮为交易蓝。

### 交互与动效

下拉、日期选择和 Tab 沿用 Ant Design 中文控件。下拉层使用 160ms 的向下淡入过渡；加载中的实时数据仅更新数字与决策轨道，不让整页重排。系统启用减少动态效果时，停止滚动或闪烁，仅保留状态文字。

### 可访问性与响应式标准

正文对比度满足可读性，红绿均附带正负号和文字标签，不能仅靠颜色传递盈亏。键盘焦点使用交易蓝描边。宽屏使用 12 列弹性网格，平板收为 8 列，手机单列；长证券名称、金额和策略参数可换行但不挤压相邻控件。

### 实现范围与成功标准

本轮仅重构 `src/stock_ai_agent/web.py` 的 HTML、CSS 和前端渲染，不改变数据 API、SQLite 表或交易策略。完成后应能在桌面和手机宽度下无横向溢出地展示六条收益曲线、实时持仓、完整盈亏日历、可排序排行榜和回测确认流程。

---

## 22. 决策账本重构（已确认）

本次重构以“市场账本”替代通用深色行情终端。页面以暖白交易面、钴蓝结构线和深色文字构成高可读工作区；盈利采用红色、亏损采用绿色，色彩之外同步提供正负符号和文字状态。

最重要的界面元素是“决策轨道”：它按时间展示行情刷新、策略信号、风控结论与模拟成交，让用户不用在表格中反推 Agent 的行为。收益图保留多基准比较，并增加可点选图例、鼠标/键盘数据点读取、文字摘要和无数据说明。

交互采用 160–220ms 的透明度/位移过渡，且只作用于状态切换、下拉层和按钮反馈；不使用自动滚动行情条。页面提供手动刷新、失败提示、加载状态、跳转主内容链接、可见键盘焦点与减少动态效果适配。

## 23. 观察池添加（已确认）

在“实时持仓”标题区域提供“添加标的”入口，而不是新增一级工作 Tab。桌面端使用右侧抽屉，移动端自动切换为底部弹层；搜索框支持六码代码和名称输入，结果明确展示证券名称、规范代码和股票/ETF 类型。

新增项保存到本地持久化观察池，和默认配置标的合并后用于看板展示、历史同步和下一轮实时盯盘。代码输入优先在本地规范化为沪深证券，避免依赖外部目录；名称联想优先返回本地观察池，随后使用 AKShare 缓存的公开行情目录。外部目录不可用时不阻断代码添加。

## 24. 盈亏分析布局与日期控件微调（已确认）

收益区间、日期范围和指标类型使用固定的网格槽位，切换“当年 / 当月 / 自定义”时不改变各控件位置。宽屏保持标题左侧、控制组右侧；中等桌面宽度下标题单独占行，控制组稳定排在下一行，避免标题被压缩。

图表先呈现收益曲线，再在其下展示跑赢或跑输各大盘指数的差值卡片。日期控件统一使用正常字重和纯数字日期单元格，例如 `20`，不附加“日”。
