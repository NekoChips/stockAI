# A股沪深模拟盘 AI-Agent 第一版完成报告

## 已实现

- 默认配置：100 万人民币模拟资金，固定 ETF 池 `588170.SH`、`588200.SH`。
- 数据层：默认使用 AKShare 适配器，支持实时 ETF/股票行情解析、历史 K 线解析、缺依赖中文安装提示；必盈 API 与东方财富公开适配器保留为可选 fallback。
- 动态切换：新增 provider factory，可通过 `config/default.yaml` 中的 `data.provider` 与 `data.history_provider` 切换行情源。
- 持久化：SQLite `MarketDataStore`，按 `(symbol, interval, timestamp)` upsert 保存 K 线，接口兼容未来 MySQL/PostgreSQL 实现。
- 市场范围：只允许沪深 `SH/SZ` 股票与 ETF，拒绝非固定池标的。
- 技术指标：SMA、EMA、MACD、RSI、布林带、ATR、成交量比率、VWAP、日内位置。
- 策略层：多指标综合策略、时间序列动量、均值回归、双 ETF 相对强弱、波动率目标仓位、回撤止损/再入场。
- 聚合器：多策略投票、权重和风险惩罚，冲突时保守处理。
- 风控层：数据过期、固定标的、最大仓位、现金下限、100 股/份整数倍、T+1 可卖数量、单日交易次数。
- 模拟盘：本地现金、持仓、成本、可卖数量、手续费、滑点、已实现/未实现盈亏。
- 日报：生成本地中文 Markdown 日报。
- 学习：按策略贡献生成中文总结，参数建议默认为 `待人工确认`。

## 如何验证

```bash
PYTHONPATH=src python3 -m unittest discover -s tests
```

本轮结果：56 个测试全部通过。

## 如何生成收盘日报

```bash
PYTHONPATH=src python3 -m stock_ai_agent.app post-close --reports reports
```

已生成样例：`reports/daily_reports.md`

## 如何同步历史 K 线

```bash
PYTHONPATH=src python3 -m stock_ai_agent.app sync-history
```

默认写入：`data/stock_ai_agent.sqlite3`

使用 AKShare 前需要安装依赖：

```bash
python3 -m pip install akshare
```

此前使用东方财富适配器已真实同步成功：

- `588170.SH`：332 条历史 K 线。
- `588200.SH`：635 条历史 K 线。

## 已知限制

- 当前环境尚未安装 AKShare；已完成 fixture 测试和动态 provider 切换，未在本轮真实联网验证 AKShare。
- 必盈 API 仍可作为 fallback，但需要有效 licence。
- 东方财富公开适配器保留为 fallback，但公开网页端点仍可能出现远端断连或字段变化。
- 当前只持久化历史 K 线，后续建议继续持久化实时行情、信号、订单、成交和日报索引。
- MySQL/PostgreSQL 尚未实现具体适配器，但已有 `MarketDataStore` 接口隔离存储细节。
- 本项目仅用于模拟盘研究，不构成投资建议，也不连接实盘交易。

## 2026-08-19 Web 界面重构

- 重构为蓝白色“策略执行台”：将账户总览、盈亏分析、决策轨道、持仓、盈亏日历、排行榜和回测确认组织为可持续盯盘的工作界面。
- 盈亏分析支持 Agent、上证指数、深证成指、沪深 300、创业板指与科创 50 的多曲线比较；可切换收益额、收益率、资产，并可点选图例查看或隐藏曲线。
- 增加手动刷新反馈、加载与失败状态、可见键盘焦点、减少动态效果适配，以及 375px 移动端布局。

本轮验证：`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest discover -s tests`，75 个测试通过；桌面与 375px 视口实测无横向溢出。

## 2026-08-19 观察池添加

- “实时持仓”新增“添加标的”抽屉入口，支持六位证券代码、名称联想、股票/ETF 类型展示和沪深市场校验。
- 手动添加的标的保存到 SQLite，并自动合并到后续历史同步和实时盯盘的有效交易宇宙。
- 新增 `GET /api/watchlist/search` 与 `POST /api/watchlist`，保存成功后返回刷新后的看板数据。

本轮验证：78 个单元测试通过；`600519` 的代码搜索已实测规范化为 `600519.SH`。当前网络对 AKShare 东方财富目录请求有限制，名称联想会优先使用本地观察池与后续可用的 AKShare 缓存。

## 2026-08-20 证券目录、删除标的与基准收益修复

- 新增 SQLite `instrument_catalog` 全量目录快照与每日后台同步机制；代码或名称搜索从本地目录返回正式中文名称。
- AKShare 目录同步添加备用源：股票从东方财富失败时改用腾讯，ETF 从东方财富失败时改用同花顺；同步过程中不在 Agent 内存常驻全市场列表。
- 观察池新增删除操作，持仓中的标的会被保护，不能直接移除；默认配置标的通过排除表隐藏，不修改 YAML 配置。
- 修复腾讯 `sh600519`、`sz000001` 格式被过滤的问题；实际同步 5,210 只股票和 1,579 只 ETF，合计 6,789 条。
- 修复指数收益全部为零：移除缺失指数的零值补齐，完成五个主要指数各 637 条 K 线同步，接口现在返回真实曲线和同步状态。

本轮验证：`PYTHONPATH=src python3 -m unittest discover -s tests -v`，83 个测试通过。

## 2026-08-20 收益分析与观察池交互修复

- 修复观察池删除按钮和收益图图例无响应的问题，动态按钮现统一使用事件监听。
- 盈亏分析新增当年、当月与自定义日期范围；每个区间都以起始日 0 收益率重新计算 Agent 与指数曲线。
- 新增五个主要指数的最新跑赢/跑输进度条，显示 Agent 收益率、指数收益率和差值。
- 去除图表下方可见的“已展示”文本，保留无障碍文字说明。
- 修复中文校验错误导致 API 断开的问题，改为 JSON 错误响应。

本轮验证：86 个单元测试通过；浏览器实测图例隐藏、当月区间和自定义日期范围控件。

## 2026-08-20 盈亏分析布局与日期控件优化

- 固定“当年 / 当月 / 自定义”、日期范围和指标选择器的网格位置；中等宽度桌面自动将标题与筛选器分行，避免标题被挤压。
- 跑赢/跑输指数对比卡片移动到收益图下方。
- 日期弹层改为正常字重和纯数字日期，不再显示 `20日` 形式。

本轮验证：86 个单元测试通过；浏览器验证各区间切换不改变控件坐标，日期弹层不含“日”后缀。
