# 代码审查报告

## 结果

未发现阻塞问题。

## 已审查重点

- 没有真实券商接入或实盘下单代码，订单仅由 `PaperBroker` 在本地模拟成交。
- 数据过期会在风控层被拒绝，不能生成新的模拟订单。
- 固定交易宇宙只允许已配置的沪深 ETF：`588170.SH`、`588200.SH`。
- 默认行情源已切换为 AKShare，provider factory 支持通过配置切换到必盈、东方财富或后续新增数据源。
- AKShare 未安装时会给出中文安装提示；必盈 API licence 不写死在代码中，仍可通过环境变量或配置提供。
- SQLite 仅通过 `MarketDataStore` 仓储接口暴露给应用层，策略和风控不依赖 SQLite 细节。
- 历史 K 线以 `(symbol, interval, timestamp)` 唯一键 upsert，重复同步不会产生重复行。
- 单一技术指标或单一量化策略不会直接下单，所有策略先聚合，再经过风控。
- 学习模块的策略调整建议默认状态为 `待人工确认`。
- Markdown 日报为中文输出。

## 验证

- 命令：`PYTHONPATH=src python3 -m unittest discover -s tests`
- 结果：56 个测试全部通过。

## 剩余风险

- 当前环境尚未安装 AKShare；已用 fixture 覆盖解析和缺依赖错误路径，但未真实联网验证 AKShare。
- 必盈 API fallback 需要有效 licence；当前已用 fixture 覆盖解析和配置错误路径。
- 东方财富公开适配器仍保留为 fallback，但公开网页端点可能出现远端断连或字段变化。
- 第一版回放框架是轻量模型，未实现完整盘口撮合、停牌、涨跌停无法成交等复杂情形。
- 当前仅历史 K 线写入 SQLite；实时行情、信号、订单和成交仍主要停留在内存和 Markdown 报告中。

## 2026-08-19 决策账本界面重构审查

未发现阻塞问题。

- 看板模板已从 Python 内嵌字符串拆分为独立 HTML，数据 API 与交易、持仓、回测确认逻辑未改变。
- 图表对 Agent 与五个大盘基准使用不同颜色和线型，图例支持显示切换，并提供文字摘要、悬浮读数和键盘读取入口。
- 375px 与桌面宽度实测均无横向溢出；账户概览、图表、决策轨道和回测流程会按单列或多列重排。

### 测试缺口

- 图表数据取决于本地历史 K 线与外部行情源，本轮以零数据状态和结构交互验证为主；联网环境应继续验证全部指数曲线的非零数据表现。
- Ant Design、React、Dayjs 与 Lucide 当前通过 CDN 加载；离线部署前应改为项目锁定依赖或提供本地静态资源。

## 2026-08-19 观察池添加审查

未发现阻塞问题。

- 手动标的以独立 SQLite 表持久化，默认配置与手动观察池合并后才进入历史同步和实时盯盘，未放宽沪深 A 股/ETF 的市场限制。
- 六码输入在服务端规范化并重新校验证券类型，客户端不能把普通股票伪装成 ETF；重复添加默认标的不产生重复观察项。
- 持仓表会读取服务端观察池，而非把 ETF 名称写死在页面脚本中。

### 测试缺口

- 当前网络代理拒绝 AKShare 的东方财富目录请求，已验证本地联想与六码输入兜底；应在可访问公开行情目录的网络环境继续验证全市场中文名称联想。

## 2026-08-20 目录、观察池与大盘数据审查

未发现阻塞问题。

- 证券目录以 SQLite 全量快照持久化，Web 搜索只访问本地数据库，不会在每次输入时拉取全市场行情或占用常驻全量内存。
- 删除观察池标的会写入排除表，默认配置项也能移除；若仍有实际持仓，服务端拒绝删除以避免运行中的策略失去持仓管理对象。
- AKShare 股票目录的 `sh`/`sz` 前缀已规范化。东方财富目录受阻时，股票转腾讯、ETF 转同花顺，实测写入 6,789 个沪深股票/ETF 名称。
- 之前的指数全零来自本地没有指数 K 线且接口补齐了零值点；该补齐逻辑已删除。五个指数各同步 637 条 K 线，比较数据包含 Agent 与五个实际基准系列。

### 剩余风险

- 公共行情源仍可能因代理、限流或字段变化失败。页面会显示“待同步”，后台会在下一日或服务重启时重试；部署环境宜配置稳定的数据服务作为后续 provider。

## 2026-08-20 收益分析交互审查

未发现阻塞问题。

- 原图例和删除按钮的 HTML 内联调用嵌入了未转义双引号，浏览器无法解析属性；现已改为渲染后事件监听，并通过浏览器验证图例状态会切换。
- 所选日期区间仅影响收益图与大盘比较，盈亏日历和账户日内指标仍保持完整数据范围。
- 跑赢/跑输以各指数最新有效交易日与同日 Agent 收益作差，规避了周末或节假日造成的错误比较。
- 请求参数校验错误以 UTF-8 JSON 返回，避免 Python 标准库 HTTP 状态行不支持中文而断开连接。

## 2026-08-20 收益分析布局与日期控件审查

未发现阻塞问题。

- 当月、自定义切换后的收益区间、日期范围、指标选择器浏览器坐标保持一致。
- 跑赢/跑输模块位于收益图之后，符合由趋势到比较结论的阅读顺序。
- Ant Design 日期单元格为纯数字且字体计算字重为 `400`，避免日期内容误读为强调文本。
